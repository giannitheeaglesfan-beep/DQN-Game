# Convenience list for quick local setup (`pip install -r requirements.txt`).
# pyproject.toml is the canonical dependency list for the published package.
torch
gymnasium
pygame-ce  # pygame's own PyPI wheels don't yet support this Python version; pygame-ce is a drop-in replacement (same `import pygame`)
numpy
fastapi
uvicorn
pydantic
